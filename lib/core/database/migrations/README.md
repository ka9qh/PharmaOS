# مجلد الترحيلات (Migrations)

كل تعديل مستقبلي على بنية قاعدة البيانات بعد الإصدار الأول
يجب أن يُسجَّل هنا كملف ترحيل منفصل مرقّم، حتى لا تُفقد بيانات الصيدليات
عند تحديث النظام لاحقًا (مهم جدًا لأن النظام سيُباع كمنتج).

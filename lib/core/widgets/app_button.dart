import 'package:flutter/material.dart';
// زر موحد الشكل عبر كل شاشات النظام
class AppButton extends StatelessWidget {
  const AppButton({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink(); // TODO
}
